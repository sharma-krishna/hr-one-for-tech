// import { createSelector } from 'reselect';
// import { initialState } from './reducer';

// // const selectAttendance = state => state.get('att',initialState);
// const selectGlobal = state => state.get('global');

// export const makeSelectAttendence = () => 
//     createSelector(selectGlobal, globalState => globalState.get('attendance'));